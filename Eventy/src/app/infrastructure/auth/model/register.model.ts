export interface RegisterData {
  profilePictures: string[];
  email: string;
  password: string;
  confirmedPassword: string;
  firstName?: string;
  lastName?: string;
  name?: string;
  description?: string;
  address: string;
  phoneNumber: string;
}
